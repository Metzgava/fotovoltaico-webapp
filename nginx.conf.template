server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    auth_basic "Impianto Fotovoltaico";
    auth_basic_user_file /etc/nginx/.htpasswd;

    location / {
        try_files $uri $uri/ =404;
    }
}
